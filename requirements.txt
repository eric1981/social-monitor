# Social Monitor 依赖
# 安装: pip install -r requirements.txt
# requires-python: >=3.10, <3.13

# ── 浏览器自动化（核心） ──
patchright==1.58.2
playwright>=1.58.0

# ── 工具库 ──
loguru==0.7.3
opencv-python>=4.13.0.92
qrcode==8.2
requests==2.32.3
segno>=1.6.6

# ── 前端 API 服务（可选: server.py） ──
Flask[async]==3.1.1
flask-cors==6.0.0
